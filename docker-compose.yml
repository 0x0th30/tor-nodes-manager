services:
  api:
    #build: .
    build:
      dockerfile: Dockerfile-dev
    ports:
      - 3000:3000
    env_file:
      - config/.env
    depends_on:
      - mongodb
    links:
      - mongodb

  mongodb:
    image: mongo
    ports:
      - 27017:27017
